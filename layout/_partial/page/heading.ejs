<div class="page-heading">
  <!-- page-type-title -->
  <% if (!is_post()) { %>
    <%- partial('title') %>
  <% } %>
  <!-- page-type-slogan -->
  <h5 class="slogan font-w-lighter text-gray">
    <% if (is_archive()) { %>
    <%- theme.slogan.archives %>
    <% } else if (is_page()) { %>
    <%- theme.slogan.pages %>
    <% } else {%>
    <%- theme.slogan.posts %>
    <% } %>
  </h5>
</div>